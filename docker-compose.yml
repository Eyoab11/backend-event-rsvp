version: "3.9"

services:
  app:
    image: saulgoodmn/event-rsvp-api:latest
    container_name: event-rsvp-api
    restart: always
    ports:
      - "4002:3002"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:15
    container_name: event-rsvp-db
    restart: always
    environment:
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
      POSTGRES_DB: event_rsvp
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  postgres_data:
